# Nexus Full — variables de entorno del servidor local
# Copia este archivo a `.env` solo si necesitás cambiar el puerto.
# El archivo `.env` NO debe compartirse ni subirse a GitHub (ya está en .gitignore).

# Puerto HTTP del servidor local (por defecto 4050 vía iniciar-full.vbs)
PORT=4050
